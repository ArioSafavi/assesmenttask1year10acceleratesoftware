Requirments:

Python 3.0, 
requests, 
json, 
urllib.request, 
datetime, 
matplotlib, 
internet access, 
(feel free to replace my API key I used two different keys but it should be pretty easy to replace)